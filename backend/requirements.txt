fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
python-multipart==0.0.9
python-dotenv==1.0.1


# Extraction pipeline
pdfplumber==0.11.4
python-docx==1.1.2

# Dev-only: used to generate synthetic test fixtures, not needed at runtime
reportlab==4.2.5

# Optional: zero-shot assignment-type classification (see
# app/services/zero_shot_classifier.py). NOT installed by default — these
# are heavy (torch alone is 500MB+) and the classifier is opt-in via the
# SYLLABUSSYNC_USE_ZERO_SHOT env var. Install separately if you want to use
# it: pip install -r requirements-ml.txt
#
# transformers==4.44.2
# torch==2.4.1
